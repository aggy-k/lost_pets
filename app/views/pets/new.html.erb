<%= render 'form', pet: @pet %>


<!-- def form_for(model, options={})
  # Pet
  if object.id.present?
    proceed to update

  else
    proceed to create
  end
  .......

  if there's a block
    then yield
end -->
